import { FC } from 'react';
import { Typography } from '@mui/material';

export const WelcomePage: FC = () => {
  return (
    <Typography variant={'h1'}>Welcome to our lunch and learn!</Typography>
  );
};
